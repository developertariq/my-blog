<h1>Here are the posts for <%= @user.name %>:</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <%= link_to post.title, user_post_path(@user, post) %>
      <%= post.text %>
    </li>
  <% end %>
</ul>
