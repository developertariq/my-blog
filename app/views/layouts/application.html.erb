<!DOCTYPE html>
<html>
  <head>
    <title>MyBlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <ul >
      <% if current_user %>
        <li> <%= link_to "Edit Account", edit_user_registration_path, class: "nav-link" %></li>
        <li > <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-small btn-primary mx-2" %></li>
        <p><%= "#{current_user.name} logged in as #{current_user.role}" %></p>
      <% else %>
        <li > <%= link_to "Sign Up", new_user_registration_path, class: "nav-link"  %></li>
        <li > <%= link_to "Sign In", new_user_session_path, class: "nav-link"  %></li>
      <% end %>
    </ul>
    <p class="notice"><%= notice %></p>   
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
